const mythologyCarouselData = [
  {
    name: "Abenaki",
    title: "Mitologia Abenaki",
    category: "Povos Originários",
    image: "./assets/images/home/home-secao-mitologias/abenaki.png",
    alt: "Mitologia Abenaki",
    description:
      "Tradição espiritual ligada à natureza, aos animais sagrados, aos espíritos ancestrais e às forças criadoras do mundo.",
    url: "#",
  },
  {
    name: "Aborígene",
    title: "Mitologia Aborígene",
    category: "Tempo do Sonho",
    image: "./assets/images/home/home-secao-mitologias/aborigene.png",
    alt: "Mitologia Aborígene",
    description:
      "Cosmovisão ancestral marcada pelo Tempo do Sonho, espíritos criadores, paisagens sagradas e conexão profunda com a terra.",
    url: "#",
  },
  {
    name: "Africana",
    title: "Mitologia Africana",
    category: "Reinos Ancestrais",
    image: "./assets/images/home/home-secao-mitologias/africana.png",
    alt: "Mitologia Africana",
    description:
      "Conjunto rico de divindades, espíritos, heróis culturais, animais simbólicos e forças ligadas à criação e à ancestralidade.",
    url: "#",
  },
  {
    name: "Alemã",
    title: "Mitologia Alemã",
    category: "Folclore Europeu",
    image: "./assets/images/home/home-secao-mitologias/alema.png",
    alt: "Mitologia Alemã",
    description:
      "Lendas sombrias, florestas antigas, espíritos, criaturas fantásticas e histórias populares preservadas no imaginário germânico.",
    url: "#",
  },
  {
    name: "Americana",
    title: "Mitologia Americana",
    category: "Lendas Populares",
    image: "./assets/images/home/home-secao-mitologias/americana.png",
    alt: "Mitologia Americana",
    description:
      "Criaturas misteriosas, lendas urbanas, heróis populares e relatos que moldaram o imaginário dos Estados Unidos.",
    url: "#",
  },
  {
    name: "Árabe",
    title: "Mitologia Árabe",
    category: "Desertos e Gênios",
    image: "./assets/images/home/home-secao-mitologias/arabe.png",
    alt: "Mitologia Árabe",
    description:
      "Universo de gênios, seres invisíveis, desertos encantados, contos maravilhosos e forças espirituais antigas.",
    url: "#",
  },
  {
    name: "Argentina",
    title: "Mitologia Argentina",
    category: "Lendas do Sul",
    image: "./assets/images/home/home-secao-mitologias/argentina.png",
    alt: "Mitologia Argentina",
    description:
      "Lendas populares marcadas por espíritos, entidades rurais, criaturas noturnas e narrativas tradicionais das regiões argentinas.",
    url: "#",
  },
  {
    name: "Asteca",
    title: "Mitologia Asteca",
    category: "Império Solar",
    image: "./assets/images/home/home-secao-mitologias/asteca.png",
    alt: "Mitologia Asteca",
    description:
      "Deuses solares, serpentes emplumadas, sacrifícios sagrados e forças cósmicas ligadas ao equilíbrio do universo.",
    url: "#",
  },
  {
    name: "Basca",
    title: "Mitologia Basca",
    category: "Montanhas Sagradas",
    image: "./assets/images/home/home-secao-mitologias/basca.png",
    alt: "Mitologia Basca",
    description:
      "Mitologia antiga com deusas da terra, espíritos das cavernas, gigantes e entidades ligadas às montanhas e tempestades.",
    url: "#",
  },
  {
    name: "Brasileira",
    title: "Mitologia Brasileira",
    category: "Folclore Nacional",
    image: "./assets/images/home/home-secao-mitologias/brasileira.png",
    alt: "Mitologia Brasileira",
    description:
      "Seres encantados, protetores das matas, lendas indígenas, influências africanas e criaturas populares do imaginário brasileiro.",
    url: "#",
  },
  {
    name: "Budista",
    title: "Mitologia Budista",
    category: "Iluminação",
    image: "./assets/images/home/home-secao-mitologias/budista.png",
    alt: "Mitologia Budista",
    description:
      "Seres celestiais, guardiões espirituais, ciclos de renascimento e símbolos ligados à sabedoria, compaixão e iluminação.",
    url: "#",
  },
  {
    name: "Caribenha",
    title: "Mitologia Caribenha",
    category: "Ilhas Místicas",
    image: "./assets/images/home/home-secao-mitologias/caribenha.png",
    alt: "Mitologia Caribenha",
    description:
      "Lendas insulares com espíritos, entidades marítimas, forças ancestrais e tradições populares marcadas por mistério e magia.",
    url: "#",
  },
  {
    name: "Catalana",
    title: "Mitologia Catalana",
    category: "Lendas Ibéricas",
    image: "./assets/images/home/home-secao-mitologias/catalana.png",
    alt: "Mitologia Catalana",
    description:
      "Dragões, gigantes, bruxas, seres encantados e tradições populares presentes no imaginário mítico da Catalunha.",
    url: "#",
  },
  {
    name: "Celta",
    title: "Mitologia Celta",
    category: "Druidas Antigos",
    image: "./assets/images/home/home-secao-mitologias/celta.png",
    alt: "Mitologia Celta",
    description:
      "Florestas sagradas, povos feéricos, deuses guerreiros, druidas e forças espirituais conectadas à natureza ancestral.",
    url: "#",
  },
  {
    name: "Chilena",
    title: "Mitologia Chilena",
    category: "Lendas Andinas",
    image: "./assets/images/home/home-secao-mitologias/chilena.png",
    alt: "Mitologia Chilena",
    description:
      "Tradições marcadas por criaturas marítimas, espíritos da terra, lendas mapuches e mistérios das montanhas e ilhas.",
    url: "#",
  },
  {
    name: "Chinesa",
    title: "Mitologia Chinesa",
    category: "Dragões Celestiais",
    image: "./assets/images/home/home-secao-mitologias/chinesa.png",
    alt: "Mitologia Chinesa",
    description:
      "Dragões, imortais, imperadores celestiais, espíritos guardiões e forças cósmicas ligadas à harmonia universal.",
    url: "#",
  },
  {
    name: "Colombiana",
    title: "Mitologia Colombiana",
    category: "Lendas da Terra",
    image: "./assets/images/home/home-secao-mitologias/colombiana.png",
    alt: "Mitologia Colombiana",
    description:
      "Entidades populares, espíritos da natureza, assombrações tradicionais e narrativas ligadas às florestas e montanhas.",
    url: "#",
  },
  {
    name: "Cristã",
    title: "Mitologia Cristã",
    category: "Anjos e Profecias",
    image: "./assets/images/home/home-secao-mitologias/crista.png",
    alt: "Mitologia Cristã",
    description:
      "Anjos, demônios, milagres, profecias e narrativas sagradas que influenciaram séculos de cultura, fé e simbolismo.",
    url: "#",
  },
  {
    name: "Dinamarquesa",
    title: "Mitologia Dinamarquesa",
    category: "Norte Europeu",
    image: "./assets/images/home/home-secao-mitologias/dinamarquesa.png",
    alt: "Mitologia Dinamarquesa",
    description:
      "Lendas nórdicas e folclóricas com seres das florestas, espíritos antigos, guerreiros e criaturas do imaginário escandinavo.",
    url: "#",
  },
  {
    name: "Dominicana",
    title: "Mitologia Dominicana",
    category: "Caribe Místico",
    image: "./assets/images/home/home-secao-mitologias/dominicana.png",
    alt: "Mitologia Dominicana",
    description:
      "Lendas populares marcadas por assombrações, entidades misteriosas e histórias transmitidas pelas comunidades do Caribe.",
    url: "#",
  },
  {
    name: "Egípcia",
    title: "Mitologia Egípcia",
    category: "Nilo Sagrado",
    image: "./assets/images/home/home-secao-mitologias/egipcia.png",
    alt: "Mitologia Egípcia",
    description:
      "Deuses ancestrais, rituais funerários, entidades cósmicas e forças ligadas à eternidade, morte e renascimento.",
    url: "#",
  },
  {
    name: "Escandinava",
    title: "Mitologia Escandinava",
    category: "Reinos do Norte",
    image: "./assets/images/home/home-secao-mitologias/escandinava.png",
    alt: "Mitologia Escandinava",
    description:
      "Deuses, gigantes, valquírias, monstros e mundos conectados por uma cosmologia épica de gelo, fogo e destino.",
    url: "#",
  },
  {
    name: "Escocesa",
    title: "Mitologia Escocesa",
    category: "Terras Altas",
    image: "./assets/images/home/home-secao-mitologias/escocesa.png",
    alt: "Mitologia Escocesa",
    description:
      "Lendas de lagos profundos, fadas, espíritos das terras altas, criaturas misteriosas e tradições celtas preservadas.",
    url: "#",
  },
  {
    name: "Eslava",
    title: "Mitologia Eslava",
    category: "Florestas Antigas",
    image: "./assets/images/home/home-secao-mitologias/eslava.png",
    alt: "Mitologia Eslava",
    description:
      "Deuses da tempestade, espíritos domésticos, bruxas, florestas sombrias e entidades ligadas ao inverno e à fertilidade.",
    url: "#",
  },
  {
    name: "Europeia",
    title: "Mitologia Europeia",
    category: "Lendas do Velho Mundo",
    image: "./assets/images/home/home-secao-mitologias/europeia.png",
    alt: "Mitologia Europeia",
    description:
      "Conjunto de lendas, seres mágicos, tradições pagãs, criaturas medievais e símbolos do imaginário antigo europeu.",
    url: "#",
  },
  {
    name: "Filipina",
    title: "Mitologia Filipina",
    category: "Ilhas Ancestrais",
    image: "./assets/images/home/home-secao-mitologias/filipina.png",
    alt: "Mitologia Filipina",
    description:
      "Divindades, espíritos da natureza, criaturas noturnas e tradições orais vindas das ilhas e povos filipinos.",
    url: "#",
  },
  {
    name: "Finlandesa",
    title: "Mitologia Finlandesa",
    category: "Kalevala",
    image: "./assets/images/home/home-secao-mitologias/finlandesa.png",
    alt: "Mitologia Finlandesa",
    description:
      "Heróis mágicos, cantos sagrados, espíritos da floresta, criaturas antigas e forças ligadas à palavra e à criação.",
    url: "#",
  },
  {
    name: "Francesa",
    title: "Mitologia Francesa",
    category: "Lendas Medievais",
    image: "./assets/images/home/home-secao-mitologias/francesa.png",
    alt: "Mitologia Francesa",
    description:
      "Contos de fadas, criaturas mágicas, cavaleiros, dragões, santos lendários e tradições populares da França antiga.",
    url: "#",
  },
  {
    name: "Gaélica",
    title: "Mitologia Gaélica",
    category: "Povos Celtas",
    image: "./assets/images/home/home-secao-mitologias/gaelica.png",
    alt: "Mitologia Gaélica",
    description:
      "Heróis lendários, fadas, deuses antigos, reinos encantados e tradições espirituais dos povos gaélicos.",
    url: "#",
  },
  {
    name: "Grega",
    title: "Mitologia Grega",
    category: "Olimpianos",
    image: "./assets/images/home/home-secao-mitologias/grega.png",
    alt: "Mitologia Grega",
    description:
      "Deuses olímpicos, titãs, heróis lendários, monstros colossais e narrativas que moldaram o imaginário ocidental.",
    url: "./mythologies/mythology-detail.html?mythology=greek",
  },
  {
    name: "Haitiana",
    title: "Mitologia Haitiana",
    category: "Loas e Mistérios",
    image: "./assets/images/home/home-secao-mitologias/haitiana.png",
    alt: "Mitologia Haitiana",
    description:
      "Tradições espirituais, entidades ancestrais, ritos simbólicos e forças sagradas ligadas ao universo cultural haitiano.",
    url: "#",
  },
  {
    name: "Havaiana",
    title: "Mitologia Havaiana",
    category: "Ilhas Sagradas",
    image: "./assets/images/home/home-secao-mitologias/havaiana.png",
    alt: "Mitologia Havaiana",
    description:
      "Heróis navegadores, espíritos ancestrais e forças naturais associadas ao oceano, aos vulcões e à criação das ilhas.",
    url: "#",
  },
  {
    name: "Heráldica Europeia",
    title: "Mitologia Heráldica Europeia",
    category: "Brasões e Feras",
    image: "./assets/images/home/home-secao-mitologias/heraldica-europeia.png",
    alt: "Mitologia Heráldica Europeia",
    description:
      "Dragões, grifos, unicórnios, leões míticos e criaturas simbólicas presentes em brasões, reinos e linhagens nobres.",
    url: "#",
  },
  {
    name: "Hindu",
    title: "Mitologia Hindu",
    category: "Cosmos Sagrado",
    image: "./assets/images/home/home-secao-mitologias/hindu.png",
    alt: "Mitologia Hindu",
    description:
      "Divindades cósmicas, avatares, demônios, ciclos de criação e destruição, sabedoria espiritual e epopeias sagradas.",
    url: "#",
  },
  {
    name: "Holandesa",
    title: "Mitologia Holandesa",
    category: "Folclore Europeu",
    image: "./assets/images/home/home-secao-mitologias/holandesa.png",
    alt: "Mitologia Holandesa",
    description:
      "Seres das águas, espíritos ancestrais, figuras folclóricas e histórias preservadas nas tradições dos Países Baixos.",
    url: "#",
  },
  {
    name: "Húngara",
    title: "Mitologia Húngara",
    category: "Lendas Magiares",
    image: "./assets/images/home/home-secao-mitologias/hungara.png",
    alt: "Mitologia Húngara",
    description:
      "Tradições antigas com aves sagradas, heróis lendários, espíritos, xamanismo e narrativas do povo magiar.",
    url: "#",
  },
  {
    name: "Inca",
    title: "Mitologia Inca",
    category: "Filhos do Sol",
    image: "./assets/images/home/home-secao-mitologias/inca.png",
    alt: "Mitologia Inca",
    description:
      "Deuses solares, montanhas sagradas, ancestrais divinizados e forças naturais ligadas ao império andino.",
    url: "#",
  },
  {
    name: "Indiana",
    title: "Mitologia Indiana",
    category: "Épicos Sagrados",
    image: "./assets/images/home/home-secao-mitologias/indiana.png",
    alt: "Mitologia Indiana",
    description:
      "Lendas ancestrais, heróis épicos, seres sobrenaturais e narrativas que atravessam diferentes tradições culturais da Índia.",
    url: "#",
  },
  {
    name: "Indonésia",
    title: "Mitologia Indonésia",
    category: "Arquipélago Místico",
    image: "./assets/images/home/home-secao-mitologias/indonesia.png",
    alt: "Mitologia Indonésia",
    description:
      "Espíritos ancestrais, dragões, seres protetores, lendas insulares e tradições sagradas do arquipélago indonésio.",
    url: "#",
  },
  {
    name: "Inglesa",
    title: "Mitologia Inglesa",
    category: "Lendas Medievais",
    image: "./assets/images/home/home-secao-mitologias/inglesa.png",
    alt: "Mitologia Inglesa",
    description:
      "Cavaleiros, criaturas lendárias, florestas encantadas, reis míticos e narrativas populares da tradição inglesa.",
    url: "#",
  },
  {
    name: "Inuit",
    title: "Mitologia Inuit",
    category: "Gelo Ancestral",
    image: "./assets/images/home/home-secao-mitologias/inuit.png",
    alt: "Mitologia Inuit",
    description:
      "Espíritos do gelo, animais sagrados, entidades marítimas e histórias moldadas pelo frio, sobrevivência e natureza extrema.",
    url: "#",
  },
  {
    name: "Iorubá",
    title: "Mitologia Iorubá",
    category: "Orixás",
    image: "./assets/images/home/home-secao-mitologias/ioruba.png",
    alt: "Mitologia Iorubá",
    description:
      "Orixás, forças da natureza, ancestralidade, destino, rituais sagrados e entidades que representam aspectos da vida humana.",
    url: "#",
  },
  {
    name: "Irlandesa",
    title: "Mitologia Irlandesa",
    category: "Ilha Encantada",
    image: "./assets/images/home/home-secao-mitologias/irlandesa.png",
    alt: "Mitologia Irlandesa",
    description:
      "Tuatha Dé Danann, heróis, fadas, reinos invisíveis e histórias celtas cheias de magia, guerra e destino.",
    url: "#",
  },
  {
    name: "Japonesa",
    title: "Mitologia Japonesa",
    category: "Yokais e Kami",
    image: "./assets/images/home/home-secao-mitologias/japonesa.png",
    alt: "Mitologia Japonesa",
    description:
      "Yokais, kami, dragões celestiais, espíritos da natureza e entidades ligadas ao equilíbrio entre mundo humano e espiritual.",
    url: "#",
  },
  {
    name: "Judaica",
    title: "Mitologia Judaica",
    category: "Mistérios Antigos",
    image: "./assets/images/home/home-secao-mitologias/judaica.png",
    alt: "Mitologia Judaica",
    description:
      "Anjos, golems, demônios, sabedoria mística, tradições simbólicas e narrativas espirituais de origem antiga.",
    url: "#",
  },
  {
    name: "Lituana",
    title: "Mitologia Lituana",
    category: "Báltica",
    image: "./assets/images/home/home-secao-mitologias/lituana.png",
    alt: "Mitologia Lituana",
    description:
      "Deuses do trovão, espíritos da terra, serpentes sagradas, florestas antigas e tradições pagãs bálticas.",
    url: "#",
  },
  {
    name: "Maia",
    title: "Mitologia Maia",
    category: "Cosmos Mesoamericano",
    image: "./assets/images/home/home-secao-mitologias/maia.png",
    alt: "Mitologia Maia",
    description:
      "Deuses criadores, jaguares sagrados, mundos subterrâneos, calendários cósmicos e narrativas de criação e destruição.",
    url: "#",
  },
  {
    name: "Malaio",
    title: "Mitologia Malaio",
    category: "Sudeste Asiático",
    image: "./assets/images/home/home-secao-mitologias/malaio.png",
    alt: "Mitologia Malaio",
    description:
      "Espíritos da selva, criaturas noturnas, entidades protetoras e narrativas tradicionais do mundo malaio.",
    url: "#",
  },
  {
    name: "Maori",
    title: "Mitologia Maori",
    category: "Polinésia",
    image: "./assets/images/home/home-secao-mitologias/maori.png",
    alt: "Mitologia Maori",
    description:
      "Deuses criadores, heróis navegadores, espíritos ancestrais e forças naturais ligadas ao mar, céu e terra.",
    url: "#",
  },
  {
    name: "Mesopotâmica",
    title: "Mitologia Mesopotâmica",
    category: "Berço dos Mitos",
    image: "./assets/images/home/home-secao-mitologias/mesopotamica.png",
    alt: "Mitologia Mesopotâmica",
    description:
      "Deuses antigos, monstros primordiais, reis lendários, dilúvios sagrados e narrativas das primeiras civilizações.",
    url: "#",
  },
  {
    name: "Mexicana",
    title: "Mitologia Mexicana",
    category: "Mesoamérica",
    image: "./assets/images/home/home-secao-mitologias/mexicana.png",
    alt: "Mitologia Mexicana",
    description:
      "Serpentes sagradas, deuses solares, lendas populares, espíritos e tradições ancestrais do México antigo e moderno.",
    url: "#",
  },
  {
    name: "Nórdica",
    title: "Mitologia Nórdica",
    category: "Ragnarok",
    image: "./assets/images/home/home-secao-mitologias/nordica.png",
    alt: "Mitologia Nórdica",
    description:
      "Deuses guerreiros, gigantes, valquírias, lobos cósmicos e o destino final dos nove mundos no Ragnarok.",
    url: "#",
  },
  {
    name: "Persa",
    title: "Mitologia Persa",
    category: "Luz e Sombra",
    image: "./assets/images/home/home-secao-mitologias/persa.png",
    alt: "Mitologia Persa",
    description:
      "Forças do bem e do caos, seres celestiais, criaturas demoníacas e batalhas cósmicas entre luz e escuridão.",
    url: "#",
  },
  {
    name: "Portuguesa",
    title: "Mitologia Portuguesa",
    category: "Lendas Lusitanas",
    image: "./assets/images/home/home-secao-mitologias/portuguesa.png",
    alt: "Mitologia Portuguesa",
    description:
      "Mouras encantadas, monstros marítimos, lendas de castelos, santos populares e tradições mágicas lusitanas.",
    url: "#",
  },
  {
    name: "Romana",
    title: "Mitologia Romana",
    category: "Império e Deuses",
    image: "./assets/images/home/home-secao-mitologias/romana.png",
    alt: "Mitologia Romana",
    description:
      "Deuses adaptados ao poder imperial, heróis fundadores, rituais públicos e símbolos de guerra, ordem e destino.",
    url: "#",
  },
  {
    name: "Romena",
    title: "Mitologia Romena",
    category: "Cárpatos",
    image: "./assets/images/home/home-secao-mitologias/romena.png",
    alt: "Mitologia Romena",
    description:
      "Lendas sombrias, espíritos, vampiros folclóricos, criaturas das montanhas e tradições populares dos Cárpatos.",
    url: "#",
  },
  {
    name: "Tibetana",
    title: "Mitologia Tibetana",
    category: "Montanhas Sagradas",
    image: "./assets/images/home/home-secao-mitologias/tibetana.png",
    alt: "Mitologia Tibetana",
    description:
      "Guardiões espirituais, divindades iradas, símbolos de iluminação, montanhas sagradas e tradições místicas profundas.",
    url: "#",
  },
  {
    name: "Tupi-Guarani",
    title: "Mitologia Tupi-Guarani",
    category: "Brasil Ancestral",
    image: "./assets/images/home/home-secao-mitologias/tupi-guarani.png",
    alt: "Mitologia Tupi-Guarani",
    description:
      "Deuses criadores, espíritos da floresta, animais sagrados e narrativas indígenas ligadas à origem do mundo.",
    url: "#",
  },
  {
    name: "Turca",
    title: "Mitologia Turca",
    category: "Lobos e Céu",
    image: "./assets/images/home/home-secao-mitologias/turca.png",
    alt: "Mitologia Turca",
    description:
      "Lobos sagrados, espíritos celestes, xamanismo, mundos superiores e tradições míticas dos povos túrquicos.",
    url: "#",
  },
  {
    name: "Umbanda",
    title: "Mitologia Umbanda",
    category: "Guias e Ancestralidade",
    image: "./assets/images/home/home-secao-mitologias/umbanda.png",
    alt: "Mitologia Umbanda",
    description:
      "Guias espirituais, ancestralidade, entidades de luz e tradições que compõem uma das expressões religiosas mais marcantes do Brasil.",
    url: "#",
  },
];

/* =====================================================
   ESTADO E CONFIGURAÇÕES DO CARROSSEL
   -----------------------------------------------------
   O movimento dos cards utiliza rolagem horizontal nativa.
   Isso preserva a inércia do toque no celular e evita o
   conflito entre pointermove, preventDefault e translateX.
===================================================== */

const MYTHOLOGY_CAROUSEL_CONFIG = Object.freeze({
  scrollEndDelay: 120,
  mouseDragDistance: 8,
  touchDragDistance: 18,
  clickReleaseDelay: 160,
  resizeDelay: 180,

  /*
   * Configurações da animação das setas.
   * A duração é calculada pela distância, respeitando estes limites.
   */
  minAnimationDuration: 420,
  maxAnimationDuration: 760,
  animationPixelsPerMillisecond: 1.05,
});

const mythologyCarouselState = {
  activeIndex: 0,
  isReady: false,
  scrollEndTimer: null,
  resizeTimer: null,
  animationFrame: null,
  isAnimating: false,
  animationToken: 0,
  pointer: {
    isPressed: false,
    isMouseDragging: false,
    hasMoved: false,
    pointerId: null,
    pointerType: "",
    startX: 0,
    startScrollLeft: 0,
    hasPointerCapture: false,
  },
};

/* =====================================================
   GERADOR DOS CARDS
===================================================== */

function createMythologyCarouselCards() {
  const track = document.getElementById("mythologyCarouselTrack");

  if (!track || !Array.isArray(mythologyCarouselData)) {
    return;
  }

  if (track.dataset.rendered === "true") {
    setupMythologyCarouselFlip();
    setupManualMythologyCarousel();
    setupMythologyCarouselDrag();
    return;
  }

  if (!mythologyCarouselData.length) {
    track.replaceChildren();
    return;
  }

  const fragment = document.createDocumentFragment();

  mythologyCarouselData.forEach((mythology, index) => {
    fragment.appendChild(createMythologyCarouselCard(mythology, index));
  });

  track.replaceChildren(fragment);
  track.dataset.rendered = "true";

  setupMythologyCarouselFlip();
  setupManualMythologyCarousel();
  setupMythologyCarouselDrag();
}

function createMythologyCarouselCard(mythology, index) {
  const name = mythology.name || "Mitologia";
  const title = mythology.title || name;
  const category = mythology.category || "Tradição Ancestral";
  const image = mythology.image || "";
  const alt = mythology.alt || title;
  const description = mythology.description || "Descrição não disponível.";
  const url = mythology.url || "#";
  const mythKey = normalizeMythologyKey(name);

  const article = document.createElement("article");
  article.className = "carousel-item";
  article.dataset.myth = mythKey;
  article.dataset.index = String(index);
  article.tabIndex = 0;
  article.setAttribute("aria-pressed", "false");
  article.setAttribute(
    "aria-label",
    `Card da ${title}. Pressione Enter ou clique para virar.`,
  );

  const flipCard = document.createElement("div");
  flipCard.className = "myth-flip-card";

  const flipInner = document.createElement("div");
  flipInner.className = "myth-flip-inner";

  const front = document.createElement("div");
  front.className = "myth-flip-front";

  const badge = document.createElement("span");
  badge.className = "myth-symbol-badge";
  badge.setAttribute("aria-hidden", "true");

  const imageElement = document.createElement("img");
  imageElement.src = image;
  imageElement.alt = alt;
  imageElement.loading = "lazy";
  imageElement.decoding = "async";
  imageElement.draggable = false;

  const frontOverlay = document.createElement("div");
  frontOverlay.className = "myth-front-overlay";

  const frontLabel = document.createElement("span");
  frontLabel.className = "myth-front-label";
  frontLabel.textContent = "Mitologia";

  const frontTitle = document.createElement("h3");
  frontTitle.textContent = name;

  frontOverlay.append(frontLabel, frontTitle);
  front.append(badge, imageElement, frontOverlay);

  const back = document.createElement("div");
  back.className = "myth-flip-back";

  const categoryElement = document.createElement("span");
  categoryElement.className = "myth-category";
  categoryElement.textContent = category;

  const backTitle = document.createElement("h3");
  backTitle.textContent = title;

  const descriptionElement = document.createElement("p");
  descriptionElement.textContent = description;

  const link = document.createElement("a");
  link.href = url;
  link.className = "myth-button";
  link.setAttribute("aria-label", `Explorar ${title}`);
  link.textContent = "Explorar";

  /*
   * O botão possui navegação própria para não depender do clique
   * delegado do card. Isso evita que o flip seja executado antes
   * da mudança de página em navegadores com transformações 3D.
   */
  link.addEventListener("pointerdown", (event) => {
    event.stopPropagation();
  });

  link.addEventListener("click", (event) => {
    event.preventDefault();
    event.stopPropagation();
    event.stopImmediatePropagation();

    if (url && url !== "#") {
      window.location.assign(url);
    }
  });

  link.addEventListener("keydown", (event) => {
    if (event.key !== "Enter" && event.key !== " ") {
      return;
    }

    event.preventDefault();
    event.stopPropagation();

    if (url && url !== "#") {
      window.location.assign(url);
    }
  });

  back.append(categoryElement, backTitle, descriptionElement, link);
  flipInner.append(front, back);
  flipCard.appendChild(flipInner);
  article.appendChild(flipCard);

  return article;
}

/* =====================================================
   NAVEGAÇÃO MANUAL POR SETAS
   -----------------------------------------------------
   - Sem autoplay;
   - Sem loop infinito;
   - As setas avançam um card por vez;
   - O arraste no mobile utiliza a rolagem nativa;
   - O card ativo é atualizado durante a rolagem.
===================================================== */

function setupManualMythologyCarousel() {
  const viewport = getMythologyCarouselViewport();
  const track = document.getElementById("mythologyCarouselTrack");
  const prevButton = document.querySelector(".mythology-carousel-prev");
  const nextButton = document.querySelector(".mythology-carousel-next");
  const dotsContainer = document.getElementById("mythologyCarouselDots");
  const cards = getMythologyCards();

  if (!viewport || !track || !cards.length) {
    return;
  }

  renderMythologyDots(cards.length, dotsContainer);

  if (!mythologyCarouselState.isReady) {
    mythologyCarouselState.activeIndex = 0;
    track.dataset.activeIndex = "0";
    mythologyCarouselState.isReady = true;

    viewport.scrollLeft = 0;

    requestAnimationFrame(() => {
      viewport.scrollLeft = 0;
      updateMythologyCarouselInterface(0);
    });
  } else {
    updateMythologyCarouselInterface(getCurrentMythologyIndex());
  }

  if (prevButton && prevButton.dataset.ready !== "true") {
    prevButton.dataset.ready = "true";
    prevButton.addEventListener("click", () => {
      moveManualMythologyCarousel(-1);
    });
  }

  if (nextButton && nextButton.dataset.ready !== "true") {
    nextButton.dataset.ready = "true";
    nextButton.addEventListener("click", () => {
      moveManualMythologyCarousel(1);
    });
  }

  if (viewport.dataset.scrollReady !== "true") {
    viewport.dataset.scrollReady = "true";
    viewport.addEventListener("scroll", handleMythologyCarouselScroll, {
      passive: true,
    });
  }

  if (window.datasetMythologyCarouselResize !== "true") {
    window.datasetMythologyCarouselResize = "true";
    window.addEventListener("resize", handleMythologyCarouselResize, {
      passive: true,
    });
  }
}

function moveManualMythologyCarousel(direction) {
  const cards = getMythologyCards();

  if (!cards.length) {
    return;
  }

  const currentIndex = getCurrentMythologyIndex();
  const nextIndex = (currentIndex + direction + cards.length) % cards.length;

  setManualMythologyActiveIndex(nextIndex, {
    scroll: true,
    behavior: getPreferredScrollBehavior(),
  });
}

function setManualMythologyActiveIndex(
  index,
  { scroll = false, behavior = "smooth" } = {},
) {
  const track = document.getElementById("mythologyCarouselTrack");
  const cards = getMythologyCards();

  if (!track || !cards.length) {
    return;
  }

  const safeIndex = clampNumber(index, 0, cards.length - 1);

  mythologyCarouselState.activeIndex = safeIndex;
  track.dataset.activeIndex = String(safeIndex);

  closeAllMythologyCards();
  updateMythologyCarouselInterface(safeIndex);

  if (scroll) {
    scrollToMythologyCard(safeIndex, behavior);
  }
}

function scrollToMythologyCard(index, behavior = "smooth") {
  const viewport = getMythologyCarouselViewport();
  const cards = getMythologyCards();
  const card = cards[index];

  if (!viewport || !card) {
    return;
  }

  const targetLeft = getMythologyCardScrollPosition(viewport, card);
  const prefersReducedMotion = window.matchMedia(
    "(prefers-reduced-motion: reduce)",
  ).matches;

  if (behavior === "auto" || prefersReducedMotion) {
    cancelMythologyCarouselAnimation();
    viewport.scrollLeft = targetLeft;
    updateMythologyActiveIndexFromScroll(index);
    return;
  }

  animateMythologyCarouselScroll(viewport, targetLeft, index);
}

function animateMythologyCarouselScroll(viewport, targetLeft, targetIndex) {
  cancelMythologyCarouselAnimation();

  const startLeft = viewport.scrollLeft;
  const distance = targetLeft - startLeft;

  if (Math.abs(distance) < 1) {
    viewport.scrollLeft = targetLeft;
    updateMythologyActiveIndexFromScroll(targetIndex);
    return;
  }

  const duration = clampNumber(
    Math.abs(distance) /
      MYTHOLOGY_CAROUSEL_CONFIG.animationPixelsPerMillisecond,
    MYTHOLOGY_CAROUSEL_CONFIG.minAnimationDuration,
    MYTHOLOGY_CAROUSEL_CONFIG.maxAnimationDuration,
  );

  const animationToken = mythologyCarouselState.animationToken + 1;
  const startTime = performance.now();

  mythologyCarouselState.animationToken = animationToken;
  mythologyCarouselState.isAnimating = true;

  viewport.classList.add("is-programmatic-scrolling");
  updateMythologyCarouselControlsBusyState(true);

  function animationStep(currentTime) {
    if (animationToken !== mythologyCarouselState.animationToken) {
      return;
    }

    const elapsed = currentTime - startTime;
    const progress = clampNumber(elapsed / duration, 0, 1);
    const easedProgress = easeInOutQuint(progress);

    viewport.scrollLeft = startLeft + distance * easedProgress;

    if (progress < 1) {
      mythologyCarouselState.animationFrame =
        window.requestAnimationFrame(animationStep);
      return;
    }

    viewport.scrollLeft = targetLeft;
    mythologyCarouselState.animationFrame = null;
    mythologyCarouselState.isAnimating = false;

    viewport.classList.remove("is-programmatic-scrolling");
    updateMythologyCarouselControlsBusyState(false);
    updateMythologyActiveIndexFromScroll(targetIndex);
  }

  mythologyCarouselState.animationFrame =
    window.requestAnimationFrame(animationStep);
}

function cancelMythologyCarouselAnimation() {
  const viewport = getMythologyCarouselViewport();

  mythologyCarouselState.animationToken += 1;

  if (mythologyCarouselState.animationFrame !== null) {
    window.cancelAnimationFrame(mythologyCarouselState.animationFrame);
  }

  mythologyCarouselState.animationFrame = null;
  mythologyCarouselState.isAnimating = false;

  viewport?.classList.remove("is-programmatic-scrolling");
  updateMythologyCarouselControlsBusyState(false);
}

function easeInOutQuint(progress) {
  return progress < 0.5
    ? 16 * progress ** 5
    : 1 - ((-2 * progress + 2) ** 5) / 2;
}

function updateMythologyCarouselControlsBusyState(isBusy) {
  const buttons = document.querySelectorAll(".mythology-carousel-arrow");

  buttons.forEach((button) => {
    button.classList.toggle("is-animating", isBusy);
    button.setAttribute("aria-busy", String(isBusy));
  });
}

function getMythologyCardScrollPosition(viewport, card) {
  const cards = getMythologyCards();
  const cardIndex = cards.indexOf(card);
  const maximumScroll = Math.max(
    0,
    viewport.scrollWidth - viewport.clientWidth,
  );

  if (cardIndex <= 0) {
    return 0;
  }

  if (cardIndex === cards.length - 1) {
    return maximumScroll;
  }

  const centeredPosition =
    card.offsetLeft - (viewport.clientWidth - card.offsetWidth) / 2;

  return clampNumber(centeredPosition, 0, maximumScroll);
}

function handleMythologyCarouselScroll() {
  const viewport = getMythologyCarouselViewport();

  if (!viewport) {
    return;
  }

  /*
   * Durante a animação controlada pelas setas, o índice já é conhecido.
   * Evita recalcular o card ativo a cada frame e elimina pequenas travadas.
   */
  if (mythologyCarouselState.isAnimating) {
    return;
  }

  window.requestAnimationFrame(() => {
    const nearestIndex = getNearestMythologyCardIndex(viewport);
    updateMythologyActiveIndexFromScroll(nearestIndex);
  });

  window.clearTimeout(mythologyCarouselState.scrollEndTimer);

  mythologyCarouselState.scrollEndTimer = window.setTimeout(() => {
    const nearestIndex = getNearestMythologyCardIndex(viewport);

    updateMythologyActiveIndexFromScroll(nearestIndex);
  }, MYTHOLOGY_CAROUSEL_CONFIG.scrollEndDelay);
}

function updateMythologyActiveIndexFromScroll(index) {
  const track = document.getElementById("mythologyCarouselTrack");
  const cards = getMythologyCards();

  if (!track || !cards.length) {
    return;
  }

  const safeIndex = clampNumber(index, 0, cards.length - 1);

  mythologyCarouselState.activeIndex = safeIndex;
  track.dataset.activeIndex = String(safeIndex);

  updateMythologyCarouselInterface(safeIndex);
}

function getNearestMythologyCardIndex(viewport) {
  const cards = getMythologyCards();

  if (!viewport || !cards.length) {
    return 0;
  }

  return cards.reduce((nearestIndex, card, index) => {
    const nearestPosition = getMythologyCardScrollPosition(
      viewport,
      cards[nearestIndex],
    );
    const currentPosition = getMythologyCardScrollPosition(viewport, card);

    const nearestDistance = Math.abs(nearestPosition - viewport.scrollLeft);
    const currentDistance = Math.abs(currentPosition - viewport.scrollLeft);

    return currentDistance < nearestDistance ? index : nearestIndex;
  }, 0);
}

function handleMythologyCarouselResize() {
  window.clearTimeout(mythologyCarouselState.resizeTimer);

  mythologyCarouselState.resizeTimer = window.setTimeout(() => {
    cancelMythologyCarouselAnimation();
    scrollToMythologyCard(getCurrentMythologyIndex(), "auto");
    updateMythologyCarouselInterface(getCurrentMythologyIndex());
  }, MYTHOLOGY_CAROUSEL_CONFIG.resizeDelay);
}

function updateMythologyCarouselInterface(activeIndex) {
  const cards = getMythologyCards();
  const prevButton = document.querySelector(".mythology-carousel-prev");
  const nextButton = document.querySelector(".mythology-carousel-next");
  const dots = Array.from(document.querySelectorAll(".myth-carousel-dot"));

  cards.forEach((card, index) => {
    const isActive = index === activeIndex;

    card.classList.toggle("is-active", isActive);

    if (isActive) {
      card.setAttribute("aria-current", "true");
    } else {
      card.removeAttribute("aria-current");
    }
  });

  updateCarouselButtonState(prevButton);
  updateCarouselButtonState(nextButton);
  updateMythologyDots(dots, activeIndex);
}

function updateCarouselButtonState(button) {
  if (!button) {
    return;
  }

  /*
   * As setas permanecem sempre disponíveis.
   * A navegação é circular: ao voltar no primeiro card,
   * o carrossel vai para o último; ao avançar no último,
   * retorna ao primeiro.
   */
  button.disabled = false;
  button.setAttribute("aria-disabled", "false");
}

function renderMythologyDots(totalCards, dotsContainer) {
  if (!dotsContainer || dotsContainer.dataset.rendered === "true") {
    return;
  }

  const fragment = document.createDocumentFragment();

  for (let index = 0; index < totalCards; index += 1) {
    const dot = document.createElement("span");

    dot.className = "myth-carousel-dot";
    dot.dataset.targetIndex = String(index);
    dot.setAttribute("aria-hidden", "true");

    fragment.appendChild(dot);
  }

  dotsContainer.replaceChildren(fragment);
  dotsContainer.dataset.rendered = "true";
}

function updateMythologyDots(dots, activeIndex) {
  dots.forEach((dot, index) => {
    dot.classList.toggle("is-active", index === activeIndex);
  });
}

/* =====================================================
   ARRASTE SUAVE — MOBILE E DESKTOP
   -----------------------------------------------------
   Mobile e dispositivos touch:
   - O navegador controla a rolagem e a inércia;
   - Nenhum preventDefault é executado durante o toque;
   - Não há translateX disputando com o gesto.

   Desktop com mouse:
   - O usuário pode clicar e arrastar;
   - O movimento altera somente scrollLeft;
   - Ao soltar, o scroll-snap aproxima o card mais próximo.
===================================================== */

function setupMythologyCarouselDrag() {
  const viewport = getMythologyCarouselViewport();
  const track = document.getElementById("mythologyCarouselTrack");

  if (!viewport || !track || viewport.dataset.dragReady === "true") {
    return;
  }

  viewport.dataset.dragReady = "true";

  viewport.addEventListener("pointerdown", handleMythologyPointerDown);
  viewport.addEventListener("pointermove", handleMythologyPointerMove);
  viewport.addEventListener("pointerup", handleMythologyPointerEnd);
  viewport.addEventListener("pointercancel", handleMythologyPointerEnd);
  viewport.addEventListener("lostpointercapture", handleMythologyPointerEnd);

  viewport.addEventListener(
    "wheel",
    () => {
      cancelMythologyCarouselAnimation();
    },
    { passive: true },
  );

  viewport.addEventListener(
    "touchstart",
    () => {
      cancelMythologyCarouselAnimation();
    },
    { passive: true },
  );

  viewport.addEventListener("dragstart", (event) => {
    event.preventDefault();
  });
}

function handleMythologyPointerDown(event) {
  if (event.button !== undefined && event.button !== 0) {
    return;
  }

  if (event.target.closest("a")) {
    return;
  }

  const viewport = getMythologyCarouselViewport();
  const track = document.getElementById("mythologyCarouselTrack");

  if (!viewport || !track) {
    return;
  }

  const pointer = mythologyCarouselState.pointer;

  cancelMythologyCarouselAnimation();
  track.dataset.suppressClick = "false";
  pointer.isPressed = true;
  pointer.isMouseDragging = event.pointerType === "mouse";
  pointer.hasMoved = false;
  pointer.pointerId = event.pointerId;
  pointer.pointerType = event.pointerType;
  pointer.startX = event.clientX;
  pointer.startScrollLeft = viewport.scrollLeft;
  pointer.hasPointerCapture = false;

  /*
   * Não capturamos o ponteiro aqui.
   * Capturar no pointerdown fazia o clique final pertencer ao viewport,
   * e não ao card, impedindo o flip.
   * A captura será ativada somente depois que um arraste real começar.
   */
}

function handleMythologyPointerMove(event) {
  const viewport = getMythologyCarouselViewport();
  const track = document.getElementById("mythologyCarouselTrack");
  const pointer = mythologyCarouselState.pointer;

  if (
    !viewport ||
    !track ||
    !pointer.isPressed ||
    pointer.pointerId !== event.pointerId
  ) {
    return;
  }

  const deltaX = event.clientX - pointer.startX;
  const dragDistance =
    pointer.pointerType === "touch"
      ? MYTHOLOGY_CAROUSEL_CONFIG.touchDragDistance
      : MYTHOLOGY_CAROUSEL_CONFIG.mouseDragDistance;

  if (!pointer.hasMoved && Math.abs(deltaX) >= dragDistance) {
    pointer.hasMoved = true;
    track.dataset.suppressClick = "true";

    if (pointer.isMouseDragging) {
      viewport.classList.add("is-dragging");

      try {
        viewport.setPointerCapture(event.pointerId);
        pointer.hasPointerCapture = true;
      } catch (error) {
        pointer.hasPointerCapture = false;
      }
    }
  }

  /*
   * Em telas touch, a rolagem e a inércia continuam nativas.
   * O controle manual de scrollLeft é usado somente no mouse.
   */
  if (!pointer.isMouseDragging || !pointer.hasMoved) {
    return;
  }

  viewport.scrollLeft = pointer.startScrollLeft - deltaX;
  event.preventDefault();
}

function handleMythologyPointerEnd(event) {
  const viewport = getMythologyCarouselViewport();
  const track = document.getElementById("mythologyCarouselTrack");
  const pointer = mythologyCarouselState.pointer;

  if (
    !viewport ||
    !track ||
    !pointer.isPressed ||
    pointer.pointerId !== event.pointerId
  ) {
    return;
  }

  const moved = pointer.hasMoved;
  const wasMouseDragging = pointer.isMouseDragging;
  const hadPointerCapture = pointer.hasPointerCapture;

  pointer.isPressed = false;
  pointer.isMouseDragging = false;
  pointer.hasMoved = false;
  pointer.pointerId = null;
  pointer.pointerType = "";
  pointer.hasPointerCapture = false;

  viewport.classList.remove("is-dragging");

  if (wasMouseDragging && hadPointerCapture) {
    try {
      viewport.releasePointerCapture(event.pointerId);
    } catch (error) {
      /* O navegador pode liberar a captura automaticamente. */
    }
  }

  if (moved) {
    const nearestIndex = getNearestMythologyCardIndex(viewport);

    updateMythologyActiveIndexFromScroll(nearestIndex);

    window.setTimeout(() => {
      track.dataset.suppressClick = "false";
    }, MYTHOLOGY_CAROUSEL_CONFIG.clickReleaseDelay);

    return;
  }

  /*
   * Clique simples: nenhum bloqueio é mantido.
   * O evento click subsequente chegará ao card normalmente.
   */
  track.dataset.suppressClick = "false";
}

/* =====================================================
   FLIP DOS CARDS — CLIQUE E TECLADO
===================================================== */

function setupMythologyCarouselFlip() {
  const track = document.getElementById("mythologyCarouselTrack");

  if (!track || track.dataset.flipDelegationReady === "true") {
    return;
  }

  track.dataset.flipDelegationReady = "true";

  /*
   * Captura links antes do listener do card. É uma proteção adicional
   * para impedir que o clique em "Explorar" seja interpretado como flip.
   */
  track.addEventListener(
    "click",
    (event) => {
      const link = event.target.closest(".myth-button");

      if (!link) {
        return;
      }

      event.stopPropagation();
    },
    true,
  );

  track.addEventListener("click", (event) => {
    const clickedLink = event.target.closest("a");
    const clickedCard =
      event.target.closest(".carousel-item") ||
      event
        .composedPath()
        .find(
          (element) =>
            element instanceof HTMLElement &&
            element.classList?.contains("carousel-item"),
        );

    if (clickedLink) {
      event.stopPropagation();
      return;
    }

    if (track.dataset.suppressClick === "true") {
      event.preventDefault();
      event.stopPropagation();
      return;
    }

    if (!(clickedCard instanceof HTMLElement) || !track.contains(clickedCard)) {
      return;
    }

    event.preventDefault();
    event.stopPropagation();
    toggleMythologyCard(clickedCard);
  });

  track.addEventListener("keydown", (event) => {
    if (event.target.closest("a")) {
      return;
    }

    const isActionKey = event.key === "Enter" || event.key === " ";
    const selectedCard = event.target.closest(".carousel-item");

    if (!isActionKey || !selectedCard || !track.contains(selectedCard)) {
      return;
    }

    event.preventDefault();
    toggleMythologyCard(selectedCard);
  });

  document.addEventListener("click", (event) => {
    if (!event.target.closest("#mythologyCarouselTrack")) {
      closeAllMythologyCards();
    }
  });
}

function toggleMythologyCard(selectedCard) {
  const isAlreadyFlipped = selectedCard.classList.contains("is-flipped");

  closeAllMythologyCards();

  if (!isAlreadyFlipped) {
    selectedCard.classList.add("is-flipped");
    selectedCard.setAttribute("aria-pressed", "true");
  }
}

function closeAllMythologyCards() {
  getMythologyCards().forEach((card) => {
    card.classList.remove("is-flipped");
    card.setAttribute("aria-pressed", "false");
  });
}

/* =====================================================
   HELPERS
===================================================== */

function getMythologyCarouselViewport() {
  const track = document.getElementById("mythologyCarouselTrack");

  return (
    document.getElementById("mythologyCarouselViewport") ||
    track?.closest(".carousel-viewport") ||
    track?.parentElement ||
    null
  );
}

function getMythologyCards() {
  return Array.from(
    document.querySelectorAll("#mythologyCarouselTrack .carousel-item"),
  );
}

function getCurrentMythologyIndex() {
  const track = document.getElementById("mythologyCarouselTrack");
  const cards = getMythologyCards();

  if (!track || !cards.length) {
    return 0;
  }

  const datasetIndex = Number(track.dataset.activeIndex);
  const fallbackIndex = mythologyCarouselState.activeIndex;

  return clampNumber(
    Number.isFinite(datasetIndex) ? datasetIndex : fallbackIndex,
    0,
    cards.length - 1,
  );
}

function getPreferredScrollBehavior() {
  return window.matchMedia("(prefers-reduced-motion: reduce)").matches
    ? "auto"
    : "smooth";
}

function clampNumber(value, min, max) {
  const number = Number(value);

  if (Number.isNaN(number)) {
    return min;
  }

  return Math.max(min, Math.min(number, max));
}

function normalizeMythologyKey(value) {
  return String(value || "")
    .trim()
    .toLowerCase()
    .normalize("NFD")
    .replace(/[\u0300-\u036f]/g, "")
    .replace(/ç/g, "c")
    .replace(/\s+/g, "-");
}

/* =====================================================
   INICIALIZAÇÃO
===================================================== */

document.addEventListener("DOMContentLoaded", () => {
  createMythologyCarouselCards();
});
